version: "3.8"

services:
  contacts-api:
    build: .
    container_name: contacts-api
    ports:
      - "8080:8080"           
    volumes:
      - .:/usr/src/app        
    working_dir: /usr/src/app
    command: nodemon server.js  
    restart: unless-stopped